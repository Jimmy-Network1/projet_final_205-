DEBUG=true
SECRET_KEY=change-me
# Comma-separated, e.g. "localhost,127.0.0.1,vente-voitures.onrender.com"
ALLOWED_HOSTS=localhost,127.0.0.1
#
# Database (PostgreSQL recommandé)
# Lancer un Postgres local: `docker compose up -d db` (voir compose.yml)
# Exemple local: postgres://vente_voitures_user:vente_voitures_password@localhost:5432/vente_voitures
# (utilise SQLite si DATABASE_URL est vide)
DATABASE_URL=

# Optionnel: force PostgreSQL (échoue si SQLite)
REQUIRE_POSTGRES=false
# Réservations (en heures) : une demande d'achat "en attente" est annulée après ce délai.
RESERVATION_TTL_HOURS=24
# Seed data (dev/demo only)
# Set true to create demo users and sample listings at startup (used by `start.sh`).
SEED_DEMO_DATA=false
